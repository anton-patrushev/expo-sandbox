import type { Theme } from "~/shared/design/themes/Theme.types";
import { Colors } from "~/shared/design/tokens/Colors.consts";

export const DarkTheme: Theme = {
  colors: {
    primary: Colors.midnightPurple,
    secondary: Colors.lightPurple,
    background: Colors.blackPearl,
    text: Colors.swanWhite,
  },
};
